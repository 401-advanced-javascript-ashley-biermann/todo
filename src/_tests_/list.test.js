'use strict';

import React from 'react';
import { shallow, mount } from 'enzyme';

import TodoList from '../components/todo/list.js';

describe('Testing TodoList Component', () => {
  // it('Should render at application start', () => {
  //   let app = mount(<TodoList />);
  //   expect(app.find('#toDoList').exists()).toBe(true);
  // });
  // it('Should contain a show / hide complete button', () => {
  //   let app = shallow(<TodoList />);
  //   expect(app.find('#toDoList').exists()).toBe(true);

  //   expect(app.state('showComplete')).toBe(true);
  // });
});